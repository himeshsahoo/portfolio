# Workspace Instructions

- Keep the portfolio self-contained unless a build tool is later added.
- Prefer concise, high-impact visual changes over large structural rewrites.
- Preserve the interactive hero, project filtering, reveal animations, and portrait area.
- If a user photo is available, use it at `assets/profile.jpg`; otherwise keep the SVG fallback.
